<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Bancos</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css">
</head>
<body>
 
    <%- include('partials/navbar') %>
    
    <div class="container mt-5">
        <h1 class="mb-4">Lista de Bancos</h1>
        <a href="/createBanco" class="btn btn-primary mb-4">Crear Banco</a>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(banco => { %>
                    <tr>
                        <td><%= banco.IdBanco %></td>
                        <td><%= banco.Nombre %></td>
                        <td>
                            <a href="/editBanco/<%= banco.IdBanco %>" class="btn btn-warning">Editar</a>
                            <a href="/deleteBanco/<%= banco.IdBanco %>" class="btn btn-danger" onclick="return confirm('¿Estás seguro de que deseas eliminar este banco?');">Eliminar</a>  
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
